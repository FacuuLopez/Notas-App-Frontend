import { StyleSheet } from "react-native";

const logginFormStyles = StyleSheet.create({
    body:{
        flex:1,
        alignItems:'center',
        justfyContent:'center',
    },
})
export default logginFormStyles;