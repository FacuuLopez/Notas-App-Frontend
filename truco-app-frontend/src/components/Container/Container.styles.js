import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  contenedor: {
    flex: 1,
    backgroundColor: "#fff",
    paddingVertical: "10%",
    paddingHorizontal: "5%",
  },
});

export default styles;
