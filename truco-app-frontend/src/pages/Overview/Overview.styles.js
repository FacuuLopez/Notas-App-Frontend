import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  button: {
    position: "absolute",
    right: 0,
    bottom: 0,
    width: 10,
    height: 10,
  },
});

export default styles;
